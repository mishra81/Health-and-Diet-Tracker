<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h1 class="text-2xl font-bold text-white">Diet plans</h1><a class="fit-button" href="{{ route('admin.diet-plans.create') }}">New plan</a></div></x-slot>
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="grid gap-4 md:grid-cols-2">@foreach($plans as $plan)<div class="fit-card"><div class="flex justify-between gap-4"><div><h2 class="font-semibold text-white">{{ $plan->name }}</h2><p class="mt-2 text-sm text-slate-300">{{ $plan->description }}</p></div><span class="text-sm text-emerald-200">{{ $plan->calorie_target }} kcal</span></div><div class="mt-4 flex gap-3"><a class="text-cyan-200" href="{{ route('admin.diet-plans.edit', $plan) }}">Edit</a><form method="POST" action="{{ route('admin.diet-plans.destroy', $plan) }}">@csrf @method('DELETE') <button class="text-rose-200">Delete</button></form></div></div>@endforeach</div><div class="mt-4">{{ $plans->links() }}</div></div>
</x-app-layout>
